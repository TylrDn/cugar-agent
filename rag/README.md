# RAG

LlamaIndex-ready ingestion and retrieval helpers live in `src/cuga/modular/rag.py` and `rag/loader.py`.

- Point to local sources in `rag/sources/` or remote URLs.
- Configure vector backend in `configs/rag.yaml` and set `RAG_ENABLED=true`.
- Example query runner: `examples/rag_query.py`.
